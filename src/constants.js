export const API_URL = 'http://localhost';

export const API_PORT = '5000';

export const API_ENDPOINT = `${API_URL}:${API_PORT}`;

export const defaultContext = {
  title: 'Home',
  meta: {
    description: 'Welcome to Asterix!',
    keywords: 'asterix',
  },
  blocks: [],
};
